import 'package:flutter/widgets.dart';

class ProfilePageWithLogin extends StatelessWidget {
  const ProfilePageWithLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
